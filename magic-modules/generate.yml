---
platform: linux

image_resource:
    type: docker-image
    source:
        repository: nmckinley/go-ruby
        tag: '1.9-2.5'

inputs:
    - name: magic-modules

outputs:
    - name: mm-output

run:
    path: magic-modules/ci/generate_terraform.sh
